export interface AppSettings {
  accessibilityMode: boolean; // Large fonts, high contrast
  neurodivergentMode: boolean; // Minimal animations, low sensory load
  simplifiedMode: boolean; // For children/seniors (e.g. replace "4.5 kg CO2" with "500 phones")
}
